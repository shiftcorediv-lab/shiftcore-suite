# Another Portal Blue/Red 公開設計

## 文書の状態

- 状態: 共通入口の構築・切替試験完了
- 利用者向け共通入口: `https://another-portal-router.shiftcore-div.workers.dev/`
- 対象: Another Portal の静的フロント公開
- 対象外: 本番データ移行、GAS/API の二重化
- 実装: 完了（Cloudflare Workers Static Assets、Service Bindingルーター、公開物生成、検証）
- 外部設定・公開: Blue／Red／Router公開、共通入口のFirebase許可、ログインProxyのCORS対応まで完了。会社ドメインと会社Cloudflareアカウントは使用しない
- Blue／Red継続運用への修正: ローカル実装・検証完了、再公開は未実行

## 2026-09-04以降の運用方針

この節を現在の運用正本とする。

運用上の呼称は **Blue／Red** とする。今後「Redへ切り替える」という指示は、内部設定の `ACTIVE_SLOT=green` へ切り替える意味として扱う。

- 内部名称は既存設定との互換性のため `blue`／`green` のまま維持する。
- BlueとRedは、どちらも同じ機能を持つ実運用ポータルとする。
- Blueは青基調、Red（内部名Green）は赤基調で表示し、稼働中の枠を視覚的に識別できるようにする。
- 一方を利用者へ提供している間に、もう一方を保守・確認できる状態を維持する。
- 利用者は常に共通入口を使う。切替時もURLとログイン元オリジンを変更しない。
- 通常公開は待機中の枠を更新・確認してからRouterを切り替え、直前の実運用枠を復帰先として残す。
- 選択中の枠が接続失敗または5xxを返した場合、Routerは待機枠へ自動退避する。正常時の切替は事前の手動操作を基本とする。
- 緊急停止画面は、BlueとRedの両方が使えない場合またはRouter設定が不正な場合だけに限定する。

## 目的

利用者の入口を一つに固定したまま、実運用中の画面とは別の公開枠で次版を確認し、安全に入れ替えられるようにする。

この仕組みは、別々のコードベースを二つ保守するものではない。同じリポジトリの承認済みコミットから作った公開物を、BlueとRedの二つの枠へ交互に配置する。

## 現状確認

2026-09-02 時点のリポジトリから、次を確認した。

- 静的フロントは GitHub Pages の `shiftcore-suite` 配下で公開されている。
- `main` が現在の公開元であり、リポジトリ内に独自の GitHub Actions 公開ワークフローはない。
- TEST／本番のAPI切替は `common/environment.js` に集約され、`shiftcore_env=staging` と同一タブのセッション情報で判定している。
- 本番APIは Cloudflare Workers と Google Apps Script の既存URLを利用している。
- `shared/js/shiftcore-urls.js` などに GitHub Pages の絶対URLが残っている。
- `apps/another-portal` は、現在の業務ポータルとは別の凍結中の仮想オフィス試作である。製品名とディレクトリ名が衝突しているため、この公開切替作業では変更しない。

## 採用する構成

```text
利用者
  |
  v
another-portal-router.shiftcore-div.workers.dev  （唯一の利用者向けURL）
  |
  v
公開先ルーター        （ACTIVE_SLOT を参照）
  |-------------------------------|
  v                               v
Blue 公開枠                      Red 公開枠
実運用または待機                 待機または実運用
  |                               |
  |-------------------------------|
                  |
                  v
       既存の本番API／本番データ

```

### 構成要素

| 要素 | 役割 |
|---|---|
| 共通入口 | 利用者が保存・共有する唯一のURL。接続先を変えてもURLを変えない |
| 公開先ルーター | Blue／Redのどちらへ接続するか決め、パスとクエリを維持して転送する |
| Blue／Red公開枠 | 同じソースから作った静的ファイルを交互に配置する。Redの内部名はGreen |
| TEST環境 | 本番データと分離した書き込み確認用として現在の仕組みを残す |

初期構成では Cloudflare Workers Static Assets に Blue／Red（内部名Green）の二つの静的公開枠を置き、Cloudflare Worker を共通入口のルーターにする。ルーターから各公開枠へは、公開URLではなくService Bindingで接続する。独自ドメインは使わない。GitHub Pages は移行完了まで既存公開先として残し、安定確認後に扱いを決める。

## 公開先の管理方法

初期版では、画面上の切替管理機能を作らない。

- ルーターの `ACTIVE_SLOT` は `blue` または `green` のみを受け付ける。
- 切替は権限を持つ管理者が、管理された公開手順から実施する。
- 切替操作には、対象スロット、Gitコミット、実行者、時刻、確認結果を残す。
- 利用者ごとの振り分けや段階公開は、初期版の対象外とする。
- 切替管理画面は、認証、権限、操作確認、監査ログを設計してから将来追加する。

## URLと認証の原則

1. 利用者を Blue／Red の別ドメインへリダイレクトしない。
2. ブラウザ上のオリジンは共通入口のまま維持する。
3. Firebase Authentication の許可ドメインへ共通入口を追加する。
4. GitHub Pages の絶対URLは、共通入口または同一オリジンの相対URLへ段階的に置き換える。
5. TEST環境のリンクでは `shiftcore_env=staging` を失わない。

この方式により、公開切替による再ログインと `auth/unauthorized-domain` の発生を避けやすくする。

## Blue／Redで保証する範囲

初期版で瞬時に戻せるのは静的フロントだけである。

| 変更内容 | Blue／Red切替で復旧可能か | 条件 |
|---|---|---|
| HTML／CSS／JavaScriptの表示不具合 | 原則可能 | API契約が旧版と互換であること |
| フロントのリンク・導線不具合 | 原則可能 | バックエンド変更を伴わないこと |
| GAS／Worker APIの不具合 | 不十分 | APIを別途戻す必要がある |
| データ形式の破壊的変更 | 不可 | 事前の互換設計またはデータ復旧が必要 |
| 誤更新・データ欠損 | 不可 | 書き込み停止、調査、データ復旧が必要 |

バックエンドを変更する場合は、少なくとも一つ前のフロントが動くAPI契約を維持する。削除や必須項目変更は、追加、移行、利用開始、旧仕様削除の順に分ける。

## 公開物の識別

各公開枠には、次を確認できるリリース情報を含める。

- 製品バージョン
- GitコミットSHA
- 公開スロット
- ビルド日時
- 環境名

利用者画面には製品バージョンのみを小さく表示し、詳細は管理者が確認できる `release.json` または診断画面へ分離する。秘密情報や内部API設定は含めない。

## 通常の公開手順

例として、Blueが実運用中の場合を示す。

1. `main` から公開候補コミットを確定する。
2. 自動テスト、構文検査、主要画面の静的確認を通す。
3. TEST環境で、ログイン、ダッシュボード、Order、Shift、勤怠、実績報告の主要導線を確認する。
4. 同じコミットから作った公開物を、待機中のRed（内部名Green）へ配置する。
5. 待機枠の自動テストと公開物検査を通す。実画面確認が必要な場合は、利用時間外に一時切替し、確認後すぐ元へ戻す。
6. 本番APIに対する確認は、最初に読み取り操作だけで行う。本番書き込み確認が必要な場合は、対象と後始末を決めて別途承認を取る。
7. 公開候補コミット、確認結果、復帰先がBlueであることを記録する。
8. `ACTIVE_SLOT` をRed（内部値 `green`）へ切り替える。
9. 共通入口から主要画面を確認し、新規エラーと操作時間を監視する。
10. 問題がなければRedを実運用、Blueを待機枠として記録する。

## 巻き戻し手順

### フロントだけの問題

1. 新規書き込み操作の案内を止める。
2. `ACTIVE_SLOT` を直前の実運用枠へ戻す。
3. 共通入口からログインと主要画面を確認する。
4. 発生時刻、症状、対象コミット、巻き戻し時刻を記録する。
5. 問題の公開物は上書きせず、調査できる状態で保持する。

### APIまたはデータに影響する問題

1. 必要に応じて書き込みを停止し、読み取り専用またはメンテナンス表示へ切り替える。
2. フロントだけでなく、対象APIとデータへの影響を確認する。
3. APIの安全な版へ戻すか、互換修正を公開する。
4. データ復旧が必要な場合は、対象範囲と復旧方法を確認してから実施する。
5. Blue／Redの切替だけで復旧したとは判断しない。

## メンテナンス表示

全面停止ではなく、可能な限り次の二段階を用意する。

- 読み取り専用: 閲覧は可能だが、保存・勤怠・報告操作を止める。
- 全面メンテナンス: ログイン前後で同じ案内を表示し、復旧見込みと連絡先を示す。

勤怠や実績報告は締刻に影響するため、停止中の代替報告方法と、復旧後の代理登録手順を運用ルールとして別途決める。

## キャッシュ対策

- HTMLは短時間で更新確認できる方針にする。
- JavaScriptとCSSは内容に応じた版番号またはファイル名を付け、旧版混在を防ぐ。
- ルーターはBlue／Redのレスポンスを不用意に同じキャッシュキーで共有しない。
- 切替後の確認では、通常読込とキャッシュを無効化した読込の両方を行う。
- Service Workerを導入する場合は、旧版を保持する期間と更新失敗時の挙動を別途設計する。

## 監視項目

最低限、次を切替前後で確認する。

- 共通入口と主要HTMLのHTTP状態
- JavaScript／CSS／画像の404
- Firebaseログイン失敗
- APIエラー率とタイムアウト
- ダッシュボード初期表示時間
- Order保存、Shift反映、勤怠、実績報告の失敗
- 表示中の製品バージョンと実運用スロットの一致

速度改善そのものはv2候補とする。ただし、タイムアウト、二重送信、保存結果不明を起こす遅さはv1の運用不具合として扱う。

## 確認環境の検証実績

2026-09-04に、Blue／Red継続運用への修正をローカルで次のように確認した。

- BlueとRedがどちらも同じ静的ポータルを200で配信
- BlueのHTMLへ青テーマ属性、RedのHTMLへ赤テーマ属性を付与
- 指定枠の接続失敗または5xx時に、もう一方へ自動退避
- 両枠が使えない場合に限り503の停止画面を返却
- 型検査、16件の自動テスト、Blue／Red／Routerの公開前検査に合格
- Chromeの `div` プロファイルで、Blueの青基調とRedの赤基調を目視確認

この修正版のCloudflare再公開と共通入口での本番確認は未実行である。

2026-09-03に、コミット `7c12919` の静的公開物で次を確認した。

- Blue／Red（内部名Green）の両枠へ201ファイルを配置
- Routerのルートからログイン入口へ正常に転送
- Firebase `shiftcore-div` で `shiftcore.div@gmail.com` のGoogleログインに成功
- ダッシュボード、人員マスター、Pick My Off、Order、Shiftの表示に成功
- BlueからRedへ切替後、同一ログインでダッシュボードを表示
- RedからBlueへ巻き戻し後、リリース情報と識別ヘッダーがBlueへ復帰
- ブラウザコンソールのエラーは主要画面で0件

ダッシュボードの勤怠・成績データは13秒以上読み込み中となる場合があり、既存API側の速度課題としてv1運用監視へ残す。本番データへの書き込み試験は行っていない。

## 実装前の確認事項

- [x] 利用する共通入口: `another-portal-router.shiftcore-div.workers.dev`
- [x] 会社ドメインを利用しない
- [x] 共通入口のFirebase許可ドメイン追加
- [ ] GitHubからCloudflareへ公開する認証方法（初回の管理者OAuthは完了、定常運用方式は未決定）
- [x] Blue／Red公開枠の命名: `another-portal-slot-blue` / `another-portal-slot-green`（Redの内部名はGreen）
- [ ] 公開切替の実行者と承認者
- [x] 本番書き込みを伴わないスモークテスト
- [ ] 勤怠停止時の代替報告手段
- [ ] API互換性の判定方法
- [ ] GitHub Pagesをいつまで復帰先として残すか

## 概算作業量

| 作業 | 目安 |
|---|---:|
| URL・環境切替箇所の棚卸しと整理 | 0.5〜1日 |
| リリース情報と静的公開物の整備 | 0.5日 |
| Blue／Red公開枠とルーターの構築 | 1〜1.5日 |
| Firebase・共通入口・キャッシュ確認 | 0.5〜1日 |
| 公開／巻き戻し試験と運用手順の確定 | 0.5〜1日 |
| 合計 | 3〜5日 |

Firebase設定変更の待ち時間は含まない。Cloudflareの料金・制限は実装開始時に公式情報で再確認する。

## 実装順序

1. ハードコードされた公開URLを整理する。
2. リリース識別情報とスモークテストを追加する。
3. Blue／Red公開枠を作成する。
4. 共通入口ルーターを構築する。
5. TEST環境で切替と巻き戻しを試験する。
6. Firebase許可ドメインを設定する。
7. 本番移行日を決め、GitHub Pagesから共通入口へ切り替える。
8. 安定確認後、GitHub Pagesの扱いを決める。

ローカル実装と共通入口の公開は完了済み。公開切替は、対象と影響を確認してから行う。
