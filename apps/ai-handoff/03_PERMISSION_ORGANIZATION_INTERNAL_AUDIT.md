# 03 権限設計 組織・直属承認 内部監査

- 監査日: 2026-08-10
- 対象: 組織階層、直属管理者、役員申請、監査ログ、Account Console保存・API契約
- 判定: ローカル実装継続可。本番反映・実効権限切替は未承認

## 2026-08-10 ローカル実装追記

- 組織階層と直属関係を検証する `organization_authorization.js` を追加した。
- 組織Shadow応答を既存の共通権限APIへ追加した。旧実効権限は引き続き正本である。
- member→leader→manager→executive、自己直属、循環、直属外承認、自己承認、役員承認者をローカルテストした。
- 決裁済みだが許容コードへ未登録だった `ordercase.case.create` を追加した。
- Account Console・ShiftBuilderの全46テスト成功、差分検査成功。
- この時点では保存API、監査ログ書込み、シート列追加、本番反映、個人別設定は未実行だった。

## 2026-08-10 保存・監査ログ実装追記

- `accountConsoleUpdateOrganizationAssignment` をローカル実装した。
- 組織版の一致、変更理由、操作者階層、対象階層、自分自身、管理系統の範囲、組織グラフを保存直前に再検証する。
- マネージャーによる別マネージャー配下の変更を `SCOPE_FORBIDDEN` で拒否する。
- `authorization_change_logs` の開始・完了ログ、イベントID、ハッシュ連結をローカル実装した。
- 開始ログを書けない場合は業務更新前に停止する。更新・完了ログ失敗時は元データへ戻し、復旧情報をScript Propertiesへ残す。
- Account Console・ShiftBuilderの全52テスト成功、差分検査成功。
- 本番の組織列・監査ログシートは未作成であり、APIは未公開。既存実効権限に変更はない。

## 2026-08-10 Account Console UI追記

- 既存アカウントを選択し、組織階層、直属管理者、役員申請承認者、変更理由を別保存できるUIをローカル実装した。
- 候補取得APIはID、表示名、組織階層、直属管理者IDだけを返す。マネージャーには自分の管理系統以外を候補として返さない。
- 同格・上位者、自分自身など編集できない対象では組織設定欄を無効化する。
- 新規利用者は既存APIでIDを発番した後、組織設定を別保存する。通常アカウント情報と組織権限の同時保存は行わない。
- 実画面の未選択・無効状態を確認した。既存CSSにより直属管理者欄と役員承認者欄が同時表示された問題を発見し、`hidden` を確実に適用するCSSへ修正した。
- 未ログイン環境ではログイン画面へ遷移するため、候補取得後の実データ画面操作は未確認。本番公開前にテストアカウントで確認する。

## 確認済み事実

- 現行 `users_master` には自由入力の `position`、`grade_role` 等があるが、権限判定用の正式な組織階層・直属管理者列はない。
- 既存 `role` とShiftBuilder旧 `manager` は、今回決めた役員・マネージャー・リーダー・メンバーとは別概念である。
- `permission_assignments` と `authorization_shadow_logs` のShadow基盤は存在する。
- 組織列、役員承認者列、正式な権限変更ログは未追加である。
- 既存の未コミット差分が複数あり、本監査では03資料以外を変更していない。

## 決裁との整合

- リーダー以上の全社閲覧と、直属管理者だけの申請承認を分離している。
- 案件登録はリーダー以上、案件編集はマネージャー以上としている。
- Shiftの配置・入替・解除は組織階層ではなく、明示的なShift編集権限で判定する。
- 既存利用者の階層・直属管理者・Shift編集権限は自動推定せず、1人ずつ手動設定する。
- 正確な位置情報の閲覧者は役員が個別に決定する。
- 役員本人の申請は自己承認を禁止し、指定された別の役員が承認する。

## 監査指摘と修正

### IA-01 開始ログと完了ログを結ぶIDが不足

- 重大度: 重大
- 状態: 修正済み
- 内容: 行IDだけでは、同じ変更に対する `started` と完了結果を安全に対応付けられなかった。
- 修正: 行ごとの `authorization_change_log_id` と、変更単位の `authorization_event_id` を分離した。

### IA-02 役員が1人だけの場合の承認経路が未定義

- 重大度: 重大
- 状態: 修正済み
- 内容: 別役員承認を必須としても、activeな役員が1人だけなら承認不能になる。
- 修正: 役員本人の申請を承認経路なしとして拒否し、自己承認やシステム管理者へフォールバックしない。役員が2人だけの場合は独立監査担当への通知を必須にした。

### IA-03 ログだけでは談合を阻止できない

- 重大度: 注意
- 状態: 設計へ反映済み
- 内容: 監査ログは発見・抑止には有効だが、承認者同士の合意そのものは止められない。
- 修正: 自己承認禁止、申請者と承認者の分離、他のactiveな役員と独立監査担当への通知、定期レビュー、ログ改ざん検知を組み合わせる。

### IA-04 Google Sheetsは複数シートを一括確定できない

- 重大度: 重大
- 状態: 修正済み
- 内容: 業務データ更新後に完了ログだけ失敗する可能性がある。
- 修正: `started` ログ、業務更新、完了ログの順とし、途中失敗を `recovery_required` として定期検出する。

## 実装前に残る業務設定

1. 初回の役員を誰にするか。
2. 各役員の `executive_reviewer_user_id` を誰にするか。
3. 承認権を持たない独立監査担当を誰にするか。
4. 正確な位置情報を閲覧できる人を役員が個別決定すること。
5. 既存利用者を1人ずつ階層・直属管理者・Shift編集権限へ割り当てること。

## 実装開始条件

- 列追加、API契約、監査ログ契約の明示承認。
- 本番 `users_master` のヘッダーと対象件数の再読取。
- 初回役員設定の移行手順とロールバック手順の承認。
- 実効権限を変えないShadow導入範囲の確定。
- 自己昇格、循環、直属外承認、役員1人、版競合、ログ失敗のテスト準備。

## 2026-08-10 本番前内部再監査

- 判定: ローカル修正後承認。本番反映は初回役員移行・ロールバック手順の確定待ち
- Account Console・ShiftBuilder全60テスト成功、構文検査・差分検査成功。

### IA-05 別系統からの付け替え

- 重大度: 重大
- 状態: 修正済み
- 内容: マネージャーが送信値を直接変更すると、別マネージャー配下のメンバーを自系統へ付け替えられる余地があった。
- 修正: 変更前と変更後の両方が操作者の管理系統内であることをAPI側で確認する。画面上も別系統は編集不可とした。

### IA-06 配下側へ発生する新規不整合

- 重大度: 重大
- 状態: 修正済み
- 内容: 配下を持つリーダー等を降格した場合、対象本人だけを検証すると配下側の直属関係不整合を見逃せた。
- 修正: 変更前後の組織グラフを比較し、今回の変更によって新しく増える全利用者のエラーを拒否する。既存の無関係な移行途中エラーだけでは保存を止めない。

### IA-07 組織管理IDの過剰公開

- 重大度: 重大
- 状態: 修正済み
- 内容: 直属管理者IDと役員承認者IDが共通権限応答とAccount Console通常一覧へ含まれていた。
- 修正: 共通応答と通常一覧から削除し、manager／executiveだけが使える専用APIへ限定した。候補APIも表示に不要な直属管理者IDを返さない。

### IA-08 組織Shadow読取失敗による旧権限巻き添え

- 重大度: 重大
- 状態: 修正済み
- 内容: 組織情報の再読取失敗が共通権限API全体を失敗させる経路があった。
- 修正: 組織情報読取を旧権限から例外隔離し、失敗時も旧実効権限を返す。

### IA-09 外部人員の内部階層混入

- 重大度: 重大
- 状態: 修正済み
- 内容: 送信値次第で外部・アライアンス人員へ内部組織階層を設定できた。
- 修正: 正式な内部人員だけを組織設定対象とし、外部人員を専用コードで拒否する。

### IA-10 組織Shadow専用の緊急停止経路が未実装

- 重大度: 重大
- 状態: 修正済み
- 内容: ロールバック案に組織Shadow停止を記載していたが、既存の権限Shadowとは独立して停止するコードがなかった。
- 修正: `ORGANIZATION_SHADOW_ENABLED` を追加し、false／0／offで組織Shadowだけを停止する。停止時も旧実効権限を返すテストを追加した。

## 本番反映候補の範囲

1. Account GASへ組織Shadow、候補取得、保存、監査ログコードを追加する。
2. `users_master` 末尾へ組織6列を追加する。既存行は空欄のままにする。
3. `authorization_change_logs` をヘッダーだけで作成する。
4. Account Consoleフロントへ組織設定欄を公開する。
5. 初回役員は別承認の移行操作で2人以上を相互承認可能な状態にする。
6. それ以外の利用者はAccount Consoleから1人ずつ手動設定する。
7. この段階では組織階層を実効権限へ使わず、旧権限を維持する。

## ロールバック候補

- 緊急停止プロパティで組織Shadow読取を停止できるようにする。
- Account Consoleフロントの組織設定欄を直前版へ戻す。
- GASを直前デプロイ版へ戻す。
- 追加列・監査ログは削除せず残し、書込みだけ停止する。
- 初回役員設定行は、監査ログと復旧用控えを照合して別承認で空欄へ戻せるようにする。

## 2026-08-10 えいち承認後の初回役員移行実装

- えいちは、ここまでの設計、ローカル実装、内部再監査結果を承認した。
- 初回役員は2人以上を1回の移行処理で設定し、2人なら相互、3人以上なら循環する別役員を申請承認者にする。
- 実行者は役員候補本人または設定済みの独立監査担当に限定し、Apps Scriptを実行しているGoogleアカウントのメールと `users_master` の登録メールが一致しなければ拒否する。独立監査担当が代行しても役員候補へは含めない。
- 停止中、外部人員、重複ID、1人だけの役員候補、既に組織設定済みの環境を拒否する。
- Web APIからは実行できず、Script Propertiesの一時設定とApps Scriptエディタからの手動実行に限定する。
- 開始・成功・失敗を監査ログへ残し、途中失敗時は全対象を元へ戻す。復元失敗は復旧プロパティへ記録する。
- 成功後は実行許可をfalseに戻し、役員ID・実行者ID・理由の一時プロパティを削除する。
- 全63テスト成功、構文検査・差分検査成功。本番実行は未承認・未実行。

## 2026-08-10 本番準備・ロールバック追記

- 不足する組織6列と空の監査ログシートだけを作る `setupOrganizationAuthorizationStorage` を追加した。
- 準備中は組織Shadowをfalseに固定し、初回役員設定成功後にtrueへ切り替える。
- 初回役員設定直後かつ他利用者未設定の場合だけ使える `runOrganizationExecutiveBootstrapRollback` を追加した。
- コードの前版復帰、組織Shadow停止、初回役員データ復元を別操作として定義した。
- 実行順と対象を `03_PERMISSION_PRODUCTION_ROLLOUT.md` に固定した。

## 2026-08-10 本番正本直接比較・リリース範囲監査

- 判定: Account GASの03送信候補は分離可能。フロントは混在差分の選別が完了するまでコミット不可
- 本番Account GASを一時領域へ読み取り取得し、ローカル正本と直接比較した。
- 既存差分は `api.js`、`authorization.js`、`config.js` の3ファイル、新規は組織・監査関連4ファイルだけだった。
- その他の本番GAS 15ファイルは一致し、無関係なGAS差分が送信候補へ混ざっていないことを確認した。
- 既存URLの直前復帰基準が第44版であることを確認した。
- `account-console.html` には03の組織設定と別作業のテーマ参照先変更が同居している。03コミットでは `origin/main` のテーマ参照を維持し、03部分だけを選択する。
- `account_console_users.js` の空白差分、共有テーマ、他アプリ画面、PMO等の未コミット差分は03対象外とした。
- この監査は読み取りと資料更新だけで、本番変更、commit、push、公開は実行していない。

## 2026-08-10 初回役員候補の照合

- えいちが初回役員2名を指定し、本番Account Consoleで両名が有効な社内利用者であることと内部IDを読み取り照合した。
- 既存の役職欄に「役員」と表示される別利用者は、今回の新組織階層の初回役員へ含めないと業務決定された。
- 既存役職、既存role、アカウント種別を新しい組織階層へ自動変換しない原則と整合する。
- 個人を識別する氏名、メールアドレス、内部IDは本資料へ記録していない。
- 読み取り確認だけで、利用者データ、権限、組織階層、本番設定は変更していない。
- 業務決定反映後にAccount Console・ShiftBuilderの全63テスト、対象JavaScriptの構文検査、差分検査を再実行し、すべて成功した。

## 2026-08-10 Claude全体監査の条件対応

- Claude全体監査の初回判定は条件付き承認だった。
- H-1: 通常のAccount Console更新が `users_master` の行全体を書き戻し、同時に保存された組織6列を監査ログなしで巻き戻し得る指摘を確認した。
- H-1修正: 通常更新と組織更新を同じScriptLockで直列化し、通常更新は許可された列と更新情報だけを列単位で保存する。組織6列を通常更新経路から書き戻さない。
- H-2: 監査ログのハッシュ連鎖は生成だけで、改変、未完了イベント、`recovery_required` を検出する検証処理が存在しない指摘を確認した。
- H-2修正: `verifyAuthorizationChangeLogIntegrity_` と手動実行用 `runAuthorizationIntegrityAudit` を追加し、ハッシュ不一致、`started` のままのイベント、`recovery_required` ログ、復旧用Script Propertyを異常として検出する。
- 中指摘修正: `ORGANIZATION_SHADOW_ENABLED` が明示的にtrueでない限り組織Shadowを停止するfail-closedへ変更した。
- 中指摘修正: 組織Shadow停止中は組織情報の読取りだけでなく、通常の組織変更APIも書込み前に拒否する。
- 修正後はAccount Console・ShiftBuilderの全70テスト、対象JavaScriptの構文検査、差分検査がすべて成功した。
- Claudeが示した全7条件のうち、要約に詳細がなかった残条件は独立再監査で確認する。承認フローの実機能接続は実効権限切替前の別ブロッカーとして維持する。

## 2026-08-11 Claude再監査の残条件対応

- Claude再監査はコード側承認相当、手順書3点を条件とする条件付き承認だった。
- R-1修正: `account_console_users.js` を03の正式な既存変更対象へ追加し、空白差分ではなくH-1のScriptLock・列単位書込みだけを含めると手順書へ明記した。
- `config.js`、`authorization.js`、`api.js`、`account_console_users.js` の変更内訳を手順書へ明記した。
- 初回役員ロールバックに使う内部IDは、実行直前の読み取り控えと監査ログの成功イベントを照合して復元する手順へ修正した。
- 追加改善: 監査ログの末尾ハッシュと行数をScript Propertyへ別保存し、末尾切り詰めを検出できるようにした。
- 追加改善: 通知先のScript Propertyを必須とする日次6時台の整合性検査トリガー作成・重複防止・削除関数を追加した。
- 修正後はAccount Console・ShiftBuilderの全72テスト、対象JavaScriptの構文検査、差分検査がすべて成功した。
- 本番変更、commit、push、公開、トリガー作成、通知送信は未実行。

## 2026-08-12 Claude承認後の予防修正

- Claude最終判定は承認。本番Shadow反映へ進行可能とされた。
- 非ブロッカーとして残った監査シート削除時の通知欠落を修正し、検証処理自体の例外も異常結果へ変換して通知を試みるようにした。
- アンカー再基線化関数を追加した。明示許可と理由を必須とし、シート内ハッシュ連鎖、未完了イベント、復旧要求がすべて正常な場合だけ現在末尾へ再設定する。
- 再基線化後は実行許可をfalseへ戻し、理由を削除する。手順書へ停止・バックアップ・別担当確認・再検査を追加した。
- 通知先の簡易形式検証と試験通知関数を追加し、本番手順で実着信確認を必須とした。

## 2026-08-12 監査通知対象・独立監査担当の業務決定

- 監査異常通知は、初回役員2名と、人員マスター上の開発管理用アカウントへ送ると業務決定された。
- 開発管理用アカウントを、初回役員2名とは別の独立監査担当として扱う。
- 個人メールアドレスをコードや資料へ固定せず、承認された内部ID一覧から送信時に人員マスターのactive利用者と現在メールを解決する方式へ変更した。
- 存在しない利用者、停止中利用者、メール形式不正を拒否し、役員2名と別の監査担当が全員含まれることを検証する。
- 本資料には氏名、メールアドレス、内部IDを記録しない。

## 2026-08-12 通知継続性の追加監査対応

- Claudeは承認を維持しつつ、通知対象1名の不整合で残る全員への通知も止まる中リスクを指摘した。
- 設定・試験通知・トリガー作成時は、従来どおり全通知対象がactiveかつ有効メールを持つことを必須とする。
- 実際の異常通知時だけは、解決できた有効宛先へ縮退送信し、無効な通知対象の件数を本文へ含める。氏名、メール、内部IDは本文へ含めない。
- 役員2名と別の独立監査担当の構成検証を、手動手順だけでなく日次トリガー作成関数へ接続した。
- 実効権限への切替時は、実装者・承認者とは別の第三者監査担当を再決裁する。今回のShadow反映における開発管理用アカウントの監査担当指定を、そのまま切替後へ自動継承しない。
- 修正後のClaude判定は承認維持で、新たな重大・高・中リスクなし。本番Shadow反映へ進行可能とされた。
- 公開直前に本番GASを再取得して直接比較し、既存4ファイルと新規4ファイルだけが差分、その他14ファイルが一致することを確認した。
- apps直下の未追跡 `.clasp.json` は過去の比較用一時フォルダを指すため、本番操作・03コミットから除外する。本番操作はAccount GAS正本フォルダ内の設定だけを使用する。

## 2026-08-12 第45版公開・緊急ロールバック

- えいちの明示承認後、危険なapps直下の `.clasp.json` を自動検出されない名前へ退避した。
- `ORGANIZATION_SHADOW_ENABLED=false` を先行設定し、Account GAS正本22ファイルをpushした。公開後に再取得し、ローカル正本との完全一致を確認した。
- 既存URLを第45版として公開したが、Account ConsoleのAPI呼出しがJSONではなくGoogle Driveの「現在、ファイルを開くことができません」HTMLを返した。Codex環境とえいちの実ブラウザの双方で再現した。
- Apps Script実行履歴では第45版のdoGet・doPostは完了扱いで、GASコード例外は確認されていない。HTML応答の根本原因は未確定。
- 後続のシート列追加、監査シート作成、役員設定、通知、トリガー、フロント公開は実行せず停止した。
- 手順書の即時停止に従い、既存URLを直前安定版の第44版へ戻した。HEADソースは第45版候補のままで、既存URLだけ第44版を参照する。
- `ORGANIZATION_SHADOW_ENABLED=false` は残置したが、第44版は組織Shadowコードを参照しないため既存実効権限へ影響しない。
- えいちの実ブラウザで第44版復帰後にAccount Console一覧が正常表示され、利用者影響の解消を確認した。
- 正常な第44版ソースを版指定で再取得し、第45版候補とマニフェストを比較した。`appsscript.json` は完全一致し、メール送信を含むOAuthスコープも第44版から存在していたため、追加スコープの再承認不足という仮説は否定した。
- 次回は既存URLへ直接再投入せず、別の検証用デプロイでping・認証・一覧を確認してから再判断する。

## 2026-08-12 第48版・本番組織Shadow初期化後の内部監査

- 判定: 承認。組織Shadow基盤は稼働し、既存実効権限は旧体系のまま維持されている。
- 第45版の検証専用デプロイでpingを確認後、既存URLを段階的に第45版、第46版、第47版、第48版へ更新した。最終の既存URLは第48版を参照する。
- 実環境で不足が判明した実行者メール参照とトリガー管理のOAuth scopeを追加した。いずれもApps Scriptの手動bootstrap・日次監査設定にだけ必要で、既存利用者の実効権限判定は変更しない。
- SheetsがISO日時の `T` を表示上の空白へ変換するため監査ハッシュが誤検知した事象を再現し、日時だけを同値正規化して検証する修正と回帰テストを追加した。監査ログ2行の連鎖自体に改ざんはなかった。
- 初回役員候補と独立監査担当が別人である業務決定に合わせ、設定済み独立監査担当によるbootstrap代行を許可した。代行者は役員へ設定されず、候補2名だけが相互承認者を持つ役員・version 1となった。
- `users_master` の組織6列追加、`authorization_change_logs` 作成、通知対象3名への試験通知、日次6時台の整合性監査トリガー作成を確認した。
- bootstrap直前の役員候補2名の組織6列が全て空欄であることを読み取り控えとして確認し、反映後は候補2名だけが役員、相互承認、version 1となったことを再読取した。
- Script Propertiesはbootstrap許可false、一時的な役員候補・実行者・理由が削除済み、組織Shadow true、監査アンカー6行一致を確認した。
- `runAuthorizationIntegrityAudit` は最終実行で正常終了した。日次トリガーは1件存在する。
- Account Consoleは一覧13件、OrderCase全操作、ShiftBuilder全管理という従来の開発管理用アカウント権限を維持し、ブラウザ警告・エラーは0件だった。
- Account ConsoleとShiftBuilderのローカルテスト80/80成功、構文検査・差分検査成功。commit、push、PRは未実行。
- 実効権限切替は未実施。承認フロー接続、役員グラフ閉路検証、M-3・M-4厳格化、第三者監査担当の再決裁を別ブロッカーとして維持する。

## 2026-08-12 第48版Claude再監査の条件対応

- F-1: bootstrapは独立監査担当が代行できる一方、一括ロールバックは対象役員本人しか実行できない非対称を確認した。ロールバックもbootstrapと同じ実行者判定へ統一し、対象役員本人または設定済みの独立監査担当が、本人メール照合を通過した場合だけ実行できるよう修正した。
- F-2: Shadow期間における独立監査担当と実装・実行担当の兼務は許容するが、実効権限切替へ自動継承しない。切替前に実装者・承認者とは別の第三者を再決裁するブロッカーを維持する。
- F-3: 試験通知はGAS送信成功まで確認済みで、通知対象3名の実受信確認は運用確認として残る。
- F-4: 代行指示者の構造化記録は実効権限切替のブロッカーではなく、将来の申請・承認機能で専用フィールドを設計する課題として分離する。現行はactorと承認理由を監査ログへ残す。
- F-1修正後のClaude再監査は承認。重大・高・中リスクなし。低所見だったロールバック時のロック取得後再検証と、実行者ID不存在時の構造化エラー化も予防修正した。
- 予防修正後はAccount ConsoleとShiftBuilderのテスト82/82、構文・差分検査が成功した。既存URLを第49版へ更新し、ping、Account Console一覧13件、旧OrderCase全操作、旧ShiftBuilder全管理、ブラウザエラー0件、組織Shadow true、bootstrap許可false、日次監査トリガー1件を再確認した。
- 開発管理用アカウントでは試験通知と誤検知時の異常通知の実受信を確認済み。残る通知対象2名の実受信確認は一旦保留し、運用確認として継続する。

## 2026-08-12 developer全権・業務名簿非表示の高圧内部監査

- 判定: 条件付き承認。コード変更は公開候補。作成済みPMO月次シートの扱いだけを本番確認前の条件として残す。
- `developer` は有効状態である限り、個別の `allowed_modules`、`ordercase_permission`、`shiftbuilder_permission` に依存せず、Dashboard全入口、Account Console、OrderCase全管理、ShiftBuilder編集、PMO管理を利用できるよう統一した。
- developerも停止中アカウントの拒否、組織設定の自己変更禁止、組織グラフ検証、監査ログを迂回しない。
- 高リスク指摘: 既存のAccount Console更新APIでは、`account_console` を持つ非developerが自分のroleをdeveloperへ変更できた。developer全権化により自己昇格経路となるため、非developerによるdeveloper新設・変更と既存developerアカウント変更をサーバー側で拒否した。developer本人による自分のrole変更も拒否した。
- 中リスク指摘: 共通権限ファイルの読込版番号が旧値のままで、ブラウザキャッシュによりDashboardだけ旧表示になる可能性があった。参照版番号を更新した。
- ShiftBuilderは候補者生成2経路に加え、配置済み表示でもusers_master上のroleを確認しdeveloperを除外する。元の配置データと監査履歴は削除しない。
- PMOはAccount Consoleの名簿APIとPMO側の受取処理でdeveloperを二重除外するため、新規月次名簿には含まれない。
- 残条件: 既存PMO月次シートは氏名・社員番号だけの静的行でroleを保持しない。推測による本番データ削除は行わず、公開後に読み取り確認し、該当行があれば別途承認を得て移行する。
- 回帰テスト93/93成功、対象JavaScript構文検査、差分検査に成功した。
- 本番GAS、Git、既存利用者、既存権限、既存シートは変更していない。

## 2026-08-15 developer権限変更の本番反映後確認

- Account GAS第50版、PMO GAS第24版、ShiftBuilder GAS第39版、OrderCase GAS第52版を既存URLへ公開した。静的フロントはmainコミット `d2b865d28094ab8eb8dfc085182bfbca58749f8d`、GitHub Pages run `31871164600` の成功を確認した。
- Account、PMO、ShiftBuilderは既存URLのping、OrderCaseは認証必須の権限APIによるJSON応答と未認証拒否を確認した。
- えいちによる実画面確認で、developer向け入口が表示された。PMOでは本人画面から月別の最新提出内容、管理側から月次一覧と申請原本を確認できる。専用の提出履歴一覧画面は現時点では存在しない。
- 反映前後の本番件数は、role=developer 1件、developer配置36件（有効扱い4件）、既存PMO月次該当行2件で一致した。既存データの削除・移行は行っていない。
- 回帰テストを反映後の作業ツリーで再実行し、97/97成功した。
- developerは全入口と各アプリの管理権限を持つ一方、ShiftBuilderの候補者・配置表示とPMOの新規月次名簿からは除外する。過去の配置・PMO月次行は監査可能性を保つため削除しない。
- 独立監査は全反映をまとめた最終監査として別主体へ依頼する。実効権限切替は、承認フロー接続、役員承認者グラフの閉路検証、第三者監査担当の再決裁が完了するまで行わない。

## 2026-08-17 本番反映後の独立監査とローカル是正

- Claude独立監査は条件付き承認。本番反映済みのdeveloper全権・自己昇格防止・配置書込み拒否・最後の役員保護・権限監査ログ・ロールバック手順は解消済みと確認された。
- H-5: `apps/.clasp.json` が比較用の古い一時正本を指して再作成されていた。自動検出されない `.clasp.production-compare-disabled.json` へ退避し、`apps/.gitignore` に `.clasp.json` を追加した。各GAS正本配下の設定は変更していない。
- H-6: 役員承認者グラフの単一閉路検証自体は維持した。単一行APIで2名から3名へ移行する途中だけ発生する `EXECUTIVE_REVIEWER_GRAPH_INVALID` はShadow健全性警告として残し、保存拒否対象から除外した。他の組織不整合は引き続き拒否する。実効権限切替前には複数役員を一括更新する専用APIを必須とする。
- M-6: 登録申請管理の共通権限読込版番号を更新する既存2ハンクを03公開対象へ含め、ロールバック対象を6ファイルへ修正した。テーマ差分は03対象外として分離する。
- L-1: PMOのdeveloper除外テストを `pmo/tests/` へ移し、Account Console・ShiftBuilder・PMOを同一の定常テスト対象へ含めた。
- M-1: 本番の有効扱いdeveloper配置4件は、重複のない4セルに存在し、いずれも非アーカイブ案件かつ必要人数0ではない。developer除外後は4セルすべてで表示上の配置人数が0となり、不足数が各1（合計4）増える。他の表示対象者は未配置である。氏名・メール・内部ID・案件IDを資料へ残さず読み取り確認し、本番データは変更していない。4行を監査履歴としてactiveのまま残すか、別承認でアーカイブするかは業務判断とする。

### 2026-08-17 M-1 本番移行

- えいちの明示承認後、対象4行を削除せず、`assignment_status=archived`、`archived=true`、理由コード `developer_roster_exclusion_migration` と理由メモを保存した。
- 同じ更新束で `shift_audit_logs` へ4行を追記し、変更前 `draft/未アーカイブ`、変更後 `archived/アーカイブ済み`、理由、操作日時、実行主体、対象IDを記録した。氏名・メール・内部ID・案件IDは本資料へ転記していない。
- 反映後の別読み取りで、developer配置は全36行、active 0行、今回の理由付きアーカイブ4行、対応する監査ログ4行を確認した。監査ログ4行はすべてbefore/afterと理由が一致した。
- 他の配置、案件、必要人数、利用者、権限は変更していない。

## 2026-08-17 Claude独立再監査の承認と低所見対応

- Claudeはコミット `2e3eb82` を読み取り専用で再監査し、判定は承認。重大・高・中の残存指摘なし、Shadow運用継続可、実効権限切替はブロッカー未消化のため不可とした。独立再実行でも103/103成功した。
- R-1: 日次の `runAuthorizationIntegrityAudit` に組織グラフ健全性検査を追加した。不整合はコード別件数だけを通知し、氏名・メール・内部IDを本文へ含めない。組織グラフの読取自体が失敗した場合も異常通知する。保存時の段階更新許容は維持した。
- R-2: 監査で特定された古い比較用一時ディレクトリと `.clasp.production-compare-disabled.json` を、復元可能なゴミ箱へ移動した。正規4系統の `.clasp.json` は変更していない。
- R-1対応後のAccount Console・ShiftBuilder・PMOテストは105/105成功、差分検査成功。
- 実効権限切替の残ブロッカーは、複数役員の一括更新API、承認フローの実機能接続、第三者監査担当の再決裁、`ordercase.case.create` の実効判定接続、通知対象2名の実受信確認である。

### 2026-08-17 低所見対応の本番反映

- Account GASの対象3ファイルだけを第51版として既存URLへ公開し、ping、版指定、再取得ソース一致を確認した。
- signup-adminの版番号2行だけをmainコミット `95c8db0` として公開し、GitHub Pages run `31971757746` の成功と実配信の参照連鎖を確認した。
- テーマその他の未コミット差分、利用者、権限、配置、案件、シートデータは変更していない。

## 2026-08-17 複数役員一括更新APIのローカル実装

- 実効権限切替ブロッカーのうち、複数役員の追加・解除・承認者付替えを1回で保存する `accountConsoleBulkUpdateExecutives` をローカル実装した。本番・Git・利用者・権限・シートデータは変更していない。
- APIは組織Shadow有効時のactiveな内部developerだけが実行でき、理由、最大20件、対象重複、自己変更、停止・外部対象、楽観ロック版を検証する。
- Script Lock取得後に操作者を実シートから再検証し、更新後のactive役員2名以上、役員承認者の単一閉路、その他の新規組織不整合がないことを全件まとめて検証する。
- 全行書込み後は組織スナップショットを全利用者分再照合する。書込み、照合、成功監査ログのいずれかが失敗した場合は全対象を変更前へ戻し、復元後も全利用者分を再照合する。復元または失敗監査ログが失敗した場合は復旧要求を残す。
- 監査ログは `organization.executive.bulk_update` と共通event ID・request IDで全変更を束ね、開始と完了結果をハッシュ連鎖へ記録する。
- 役員2名から3名への健全な一括変更、部分変更拒否、重複、版競合、実行者制限、欠落行検出、復元後照合を追加テストで確認した。Account Console・ShiftBuilder・PMOの全112テストは別途最終再実行する。
- 本APIの独立監査と本番反映は未実施。実効権限は引き続き旧判定のままである。

## 2026-08-17 OrderCase案件登録の実効判定接続

- `createCase` のサーバー側入口を登録専用判定へ分離し、従来のOrderCase編集権限に加えて、共通権限応答の実効 `modules.ordercase.capabilities` に `ordercase.case.create` があることを必須にした。
- 共通権限は案件登録の都度Account APIから取得し、応答欠落、JSON不正、capability欠落はfail-closedで登録を拒否する。画面表示だけの制御にはしていない。
- 現在のAccount APIはShadow中のため `authorization.modules` に旧実効権限を返す。既存 `all`・`edit`・developerは従来どおり案件登録でき、候補権限や組織階層へはまだ切り替わらない。
- 将来Account側で実効モジュールを候補権限へ切り替えた時点で、OrderCaseは `ordercase.case.create` を持つ利用者だけを許可する。案件編集APIは従来どおり別判定であり、登録と編集を混同しない。
- 専用テスト3件を追加し、capability許可、欠落・不正時の拒否、`createCase` の専用ガード接続を確認した。全体系115テストは最終再実行で確認する。
- ローカルのみ。OrderCase GAS、本番案件、利用者、権限、Gitは変更していない。

### 直属承認接続の再確認

- 04の現行勤怠GASには `reviewRequest` と申請シートが既に存在するため、コードとREADMEを読み取り確認した。
- 現行申請行は申請者メール・氏名を持つが `applicant_internal_user_id` とrequest versionを持たない。承認処理は申請IDで行を探して更新し、同時処理の版競合、承認直前の直属関係再読取、共通 `authorization_change_logs` への記録がない。
- メールを直属関係の正式キーへ流用すると、メール変更・表記差・既存申請で誤承認するため、03側だけの推測実装は行わない。
- 接続には04側で、申請者内部ID、申請version、承認者内部ID、既存の申請中行をどう扱うかのデータ契約と移行判断が必要。したがってこのブロッカーは04確定待ちを維持する。

### コミット697068dのClaude独立監査指摘対応

- Claudeは、既存executiveへ不正な `organization_level` を送ると正規化後に空文字となり、一括更新側が専用エラーを出さず役員階層を消せる高リスク経路を指摘した。
- `buildOrganizationCandidate_` の直後に正規化済み階層が有効であることを必須化し、不正値を `ORGANIZATION_LEVEL_INVALID` で書込み前に拒否した。
- 既存役員を不正階層で空欄化できない再発防止テストを追加した。修正後は全体系116テストとClaude独立再監査を実施する。
- Claude再監査は高リスク解消を確認した一方、一括更新API本体とOrderCase fail-closed経路がソース確認中心で、振る舞いテストが不足する中・低所見を残した。
- 一括更新API本体について、全対象更新と共通event/request IDの開始・成功ログ、成功ログ失敗時の全行復元・errorログ・ロック解放をGASモックで実行するテストを追加した。
- OrderCaseについて、共通権限APIの通信例外、JSON不正、旧編集権限があってもcreate capability欠落時の拒否を実行するテストを追加した。

### Claude最終独立再監査

- 対象は基礎コミット `697068d`、高リスク是正 `be768fb`、振る舞いテスト追加 `469db9c`。
- 最終判定は承認。前回の高・中・低指摘は全て解消、新規指摘なし。
- Claude自身がAccount Console・ShiftBuilder・PMO・OrderCaseの全テストを実行し、121/121成功、失敗・スキップ0件を確認した。
- 複数役員一括更新APIとOrderCase案件登録の実効判定接続は、コード面の本番反映候補として承認された。
- 実効権限切替は別枠であり、第三者監査担当の再決裁、通知対象2名の実受信確認、04申請データ契約確定と直属承認接続が完了するまで行わない。
- ロールバック自体の失敗、バッチ途中の書込み例外、Script Lockタイムアウトの個別モックテストは将来追加候補として残るが、コードトレース上は安全側であり本番Shadow反映のブロッカーとはしない。

### 本番正本との反映前比較

- Account GAS第51版とOrderCase GAS第52版を一時領域へ読み取り取得し、ローカル正本と対象ファイルを比較した。本番、既存URL、データは変更していない。
- OrderCaseのローカル `Api_Post.js` には、本番第52版に存在する `updateStoreMaster` 分岐が欠けていた。このままファイル単位で反映すると既存店舗マスター更新を消すため、本番の5行をローカル正本へ同期した。
- `createCase` の変更が `requireOrderCaseEditor_` から `requireOrderCaseCreator_` への1行だけとなり、既存 `updateStoreMaster` を保持するテストを追加した。
- Accountの通常単一更新APIに混ざった無意味な操作者ID変数化を取り除き、一括更新APIとaction追加だけを本番差分として残した。
- Claude確認で、復元した `updateStoreMaster` 分岐が呼ぶ実装関数群もローカル正本から欠落していることが判明した。本番第52版の `Service_StoresMaster.js` に存在する店舗位置列、管理一覧、状態正規化、店舗更新の実装をローカルへ同期し、関数定義を実行確認するテストを追加した。

## 2026-08-17 第52版／第53版本番反映後の監査記録

- 認証済みのデプロイ一覧を読み取り、Account GASの既存URLが第52版 `03 executive bulk shadow API 2026-08-17`、OrderCase GASの既存URLが第53版 `03 case create capability guard 2026-08-17` を参照していることを確認した。
- 今回の監査では利用者、権限割当、組織階層、案件、店舗、配置、シートデータを変更していない。Git pushと追加の本番変更も実行していない。
- 既存URLへのAccount pingとOrderCase未認証拒否の再確認は、実行許可レビューが2回とも時間切れとなり、別のWeb取得経路もApps Script URLを開けなかったため未確認。この失敗は本番応答の失敗ではなく監査実行環境側の制約であり、到達成功として扱わない。
- ローカル正本でAccount Console、OrderCase、ShiftBuilder、PMOの全122テストを再実行し、122成功、失敗・スキップ0件を確認した。

### 一括更新APIとOrderCase案件登録権限の差分・追加テスト

- Account第52版は `accountConsoleBulkUpdateExecutives` と専用actionを追加する。activeな内部developer、理由、最大20件、対象重複、自己変更、対象状態、楽観ロック版を検証し、全対象を1つのevent ID・request IDで監査する。
- 一括更新は、更新後のactive役員2名以上、役員承認者の単一閉路、その他の新規組織不整合を全件まとめて検証する。書込み後は全利用者分を再照合し、書込み・照合・成功ログの失敗時は全対象を復元して復元後も再照合する。
- OrderCase第53版は `createCase` だけを `requireOrderCaseCreator_` へ分離し、従来の編集権限に加えて実効 `ordercase.case.create` capabilityを必須にする。編集API、店舗マスター更新API、既存店舗マスター実装は維持する。
- OrderCaseは共通権限APIの通信例外、JSON不正、応答・capability欠落をすべて書込み前にfail-closedで拒否する。Account側がShadow中の現在は旧実効権限を返すため、既存 `all`・`edit`・developerの登録可否は従来どおりである。
- 追加テストは、一括更新の健全な3名化、部分更新拒否、不正階層、重複、版競合、実行者制限、欠落行、成功ログ、成功ログ失敗時の全行復元・errorログ・ロック解放を対象とする。
- OrderCase追加テストは、capability許可、欠落・不正応答、通信例外、JSON不正、旧編集権限があってもcreate capability欠落時の拒否、`createCase` 専用ガード接続、既存 `updateStoreMaster` 分岐と実装関数の保持を対象とする。

### H-6暫定回避とdeveloper全権の競合

- 判定: H-6暫定回避は現在のShadow運用中だけ残し、実効権限切替前に畳む。今回の監査対象では追加の本番変更を行わないため、Account第52版の挙動は変更しない。
- 現状の単一更新APIは、2名から3名へ段階更新する途中の `EXECUTIVE_REVIEWER_GRAPH_INVALID` だけを保存拒否から除外する。developerは全権によりこの単一更新APIも呼べるため、一括更新API公開後も暫定回避を恒久化すると、developerが一括更新の全件検証・一括監査を迂回できる。
- 実効権限切替前に、単一更新APIで役員の追加・解除・承認者変更を行えないようにし、役員グラフ変更を一括更新APIへ限定する。併せて、developer全権でも専用経路を迂回できない拒否テストを追加する。
- この切替前対応、第三者監査担当の再決裁、通知対象2名の実受信確認、04申請データ契約確定と直属承認接続が揃うまで、組織階層を実効権限へ切り替えない。

## 2026-08-20 developer直属承認の本番実動作確認

- Chromeの本番Account Console実画面で、ログイン中の開発管理用アカウントが利用者一覧へ表示されることを確認した。本節では既存方針に従い、社員番号と内部ユーザーIDを記録しない。
- 同画面で、対象利用者の組織階層が `リーダー`、直属管理者が開発管理用アカウントと表示され、保存完了メッセージが出ていることを確認した。開発管理用アカウント自身の組織階層、自己ブートストラップの実施記録、直属設定の操作主体は未確認である。
- 設定後に対象利用者から新規申請し、開発管理用アカウントで承認操作を行ったこと、および対象申請が承認待ち一覧から消えたことは、えいちの実機確認報告による。
- 処理後の申請状態、勤怠記録の更新、共通監査ログの `started` / `success` 対応、通知作成は未確認である。このため、承認成立およびdeveloper単独での試験完結は確定扱いせず、developerを承認者とするUI操作経路が報告上成立した範囲に判定を限定する。
- 既存申請の承認者を推測で付け替えず、直属設定後の新規申請を試験対象とする方針は維持する。
- この確認はdeveloperの全権化や実効権限切替を意味しない。H-6暫定回避の撤去、第三者監査担当の再決裁、通知対象2名の実受信確認、04申請データ契約確定と直属承認接続など、既存の切替前条件は引き続き残る。

### 経路再確認を発火できる操作主体の設計判断

- 保存済み承認者と操作主体が一致しない場合も、Account側で申請後の組織版または直属承認経路の変更を検出したときは、申請を承認せず `経路再確認` へ移す。この遷移は陳腐化した申請を承認待ちから退避する整合性回復であり、操作主体への承認権付与ではない。
- Account側が `NOT_ASSIGNED_REVIEWER` を返した場合は `経路再確認` へ移さず、申請中・申請版を維持する。保存済み承認者と異なる利用者による試行について、この負例を回帰テストで固定する。
- いずれの経路でも割当外の操作主体による承認・却下は成立しない。経路変更の検出主体と結果は共通監査ログへ残す既存契約を維持する。

## 2026-08-20 勤怠GAS第9版・経路再確認の本番実動作確認

- PR #36のmerge commit `f652807a0da866dab7066647df4a3d889b83d496` をGitHub main正本として確認し、勤怠GASの既存WebアプリURLを第8版から第9版 `03 persist changed attendance approval routes 2026-08-20` へ更新した。復帰基準は第8版とする。
- 第9版を別の一時領域へ版指定で再取得し、`コード.js` がmain正本の `Code.gs` とSHA-256完全一致、`appsscript.json` が第8版から無変更であることを確認した。既存URLへのGETは `ok=true`、`service=shiftcore-attendance` のJSONを返した。
- えいちの実機操作報告では、旧承認経路を保持する既存申請に対して承認を1回だけ試行し、再読込後に承認待ち一覧から消えた。
- 操作後の勤怠管理シートを読み取り確認し、対象申請は承認済みにせず `申請中` から `経路再確認` へ移り、`request_version` は1から2へ更新されていた。既存申請の承認者を新しい直属管理者へ自動付替えしていない。
- 同時刻の共通監査ログには、操作主体を持つ `approval.review` の `rejected`、`APPROVAL_ROUTE_CHANGED` が新しい監査イベントとして記録されていた。以前の拒否イベントとは別であり、承認成功ログは作成されていない。
- この確認で成立したのは、陳腐化した既存申請を承認せず経路再確認へ退避することまでである。対象申請を再び承認可能にするには、既存方針どおり本人確認後の新規申請が必要であり、自動変換・自動付替えは行わない。
- Codexが行った本番変更は勤怠GASの第9版公開だけである。えいちの試験操作後に、対象申請1件の状態・申請版・処理日時と、対応する共通監査ログ1件の追加を読み取り確認した。利用者、権限、組織設定、勤怠記録、通知、その他の申請データは本確認の対象外であり、変更なしとは推測しない。

## 2026-08-20 直属承認正常系の本番読み取り確認と現行切替条件

- `2026-08-20 developer直属承認の本番実動作確認` で未確認としていた正常承認の結果を、Codexがその後に本番シートから読み取り確認した。この後続確認により、同節の「承認成立を確定扱いしない」という当時の判定を更新する。
- 直属設定後の新規申請は承認済み・申請版2となり、共通監査ログには同一監査イベントの `started` / `success` が記録され、申請結果通知も作成されていた。対象申請は勤怠記録IDを持たず、実装契約どおり勤怠記録行の更新対象外だった。
- 申請者内部ID、申請版、保存済み承認者ID、申請時組織版、承認直前の直属関係再確認、版一致とロック、共通監査ログ、旧申請の再申請方針について、正常承認と経路変更拒否の両方を本番で確認したため、04申請データ契約確定と直属承認接続は完了扱いとする。
- 現行の実効権限切替前条件は、H-6暫定回避の撤去、実装者・承認者とは別の第三者監査担当の再決裁、監査異常通知対象2名の実受信確認の3件である。以前の節で04申請データ契約確定と直属承認接続を残存条件として列挙した記述は、その時点の判断履歴として保持し、本節の正常系確認によって更新する。
- この更新は組織Shadowから実効権限への切替承認を意味しない。H-6はShadow中だけ維持し、切替直前に単一更新APIからの役員グラフ変更を禁止して一括更新APIへ限定し、developer全権でも迂回できない拒否テストを追加する。

## 2026-08-20 監査異常通知対象3名の実受信確認

- CodexがAccount GASの `sendAuthorizationIntegrityTestNotification` を設定済み3名を宛先として1回実行し、GAS実行ログで開始後の実行完了を確認した。件名は `[ShiftCore] 権限監査通知テスト`、本文は通知先設定テストであり実際の異常は検出されていない旨とした。
- 送信後、えいちから通知対象3名全員の実受信完了が報告された。各受信者のメールアドレス、社員番号、内部ユーザーIDは本記録へ記載しない。
- これにより、以前の節で残存条件としていた監査異常通知対象2名の実受信確認は完了扱いへ更新する。GASの送信完了はCodexの実画面確認、各受信箱への到達はえいちの確認報告であり、証拠の帰属を区別する。
- 現行の実効権限切替前条件は、H-6暫定回避の撤去と、実装者・承認者とは別の第三者監査担当の再決裁の2件である。この更新だけで実効権限切替を承認しない。

## 2026-08-21 今回の実効切替に限る監査独立性要件の例外決裁

- えいちは、今回の実効権限切替に限り、実装者・承認者とは別の第三者監査担当を置く要件を免除し、開発者が監査担当を兼務する例外を決裁した。適用対象は、H-6撤去と拒否テスト完了後に最初に行う1回の切替操作だけとし、段階的な追加切替、ロールバック後の再試行には適用しない。
- 開発者は今回の実装者かつ承認操作主体であるため、この決裁を第三者監査の完了または独立性要件の充足とは扱わない。同一主体による実装・承認・監査で欠陥や権限迂回を見逃すリスクを、えいちの例外決裁により受容する。
- この例外は今回の切替だけに適用し、恒久ルール、切替後の通常監査、次回以降の実効権限切替へ自動継承しない。次回以降は、実装者・承認者とは別の第三者監査担当を再決裁する既存原則へ戻す。
- 既存の読み取り専用独立監査結果は変更しないが、それらを組織上の第三者監査担当の代替とは扱わない。
- 独立性欠如の補償措置として、切替直後に `runAuthorizationIntegrityAudit` を手動実行し、切替操作ログと監査結果を保存する。以後7日間は日次整合性監査の成功と設定済み3名への異常通知経路を確認する。異常時は実効切替をロールバックして本例外を失効させ、再試行には新たな例外決裁または第三者監査担当の再決裁を必要とする。
- この例外決裁により、今回の切替における監査担当再決裁のブロッカーは免除扱いへ更新する。現行の実効権限切替前条件はH-6暫定回避の撤去1件であり、H-6撤去と拒否テストが完了するまで実効権限へ切り替えない。
