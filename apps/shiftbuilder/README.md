# ShiftBuilder

## テスト

リポジトリ直下から、共有認証とShiftBuilderの純ロジックテストを次のコマンドで実行する。

```bash
node --test shared/tests/*.test.mjs apps/shiftbuilder/tests/*.test.mjs
```

`node --test apps/shiftbuilder/tests/` のようなディレクトリ指定には対応していない。
