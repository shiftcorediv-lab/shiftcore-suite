# デモ2修正の確定範囲（2026-09-13）

ユーザー回答を正本とする。以下のチェックは公開・本番確認まで終えたときだけ付ける。

## 現在地（2026-09-13）

実装・マージ・公開済み：PR169 / PR170。機能公開版 `89a3aa9`、通常運用Blue。
Account API v67、Shift API v46。URL・認証設定・希望休提出データ・既存配置は変更していない。

- 関連612テスト、配信切替16テスト、配信コード型チェック成功。
- 本番：日付下人数1→2名でポップアップ、カレンダー高さ594pxを維持、通常新規の税抜を確認。登録は送信していない。
- 本番：案件・人員ヘッダーの右クリック、店舗ID指定の編集画面、候補者・配置済み人員からのルールリンク、店舗・代理店別ルール画面の読み込みを確認。ルール保存は実施していない。
- 本番：案件軸の人員情報到着後に集計更新。両軸の3行×31日が一致（10/1は提出1・受注2・過不足-1）。Consoleエラーなし。
- 本番：メンバーから本人希望の店舗一覧を取得。直属管理者ではない開発者は閲覧のみ・保存ボタン無効。実在スタッフの希望は変更していない。

### 実装済みだが本番の変更操作は未検証

- 直属管理者による本人希望の実保存、およびその値を使うアサイン注意喚起。保存許可・本人禁止・管理者変更・競合・店舗ID照合は自動テスト済み。
- 日数指定案件の充足→解除の実操作。本番9月の対象4件は未充足のため、既存配置を増減せず、描画・解除後表示を自動テストで確認した。
- 編集・反復受注の税区分保持、リセットの税抜は自動テスト済み。
- スマホ幅の実機表示は未確認（確認用ブラウザの幅変更が反映されず、PC幅での確認のみ）。

### 継続観察

ルール取得のHTML 404は同一URLで再現せず、今回の本番確認は正常に読み込めた。
読み取りだけ1回再取得する対処を追加。根本原因の解消とは判定しない。保存処理は自動再送しない。

## 受入チェック

- [ ] オーダー：日付直下の人数を入力すると日別条件ポップアップ。人数欄は小さく維持し、カレンダーを伸ばさない。1名は共通条件、2名以上は人数別時間・単価。日別メモも保持。
- [ ] 通常新規・リセットは税抜。編集・同条件受注は元案件の税区分を維持。
- [ ] 日数指定充足後：配置日に人員名、残りの日は灰色の「—」、案件左端は充足。既存配置の変更・解除を維持。
- [ ] 店舗側・代理店側の指名NGを区別。共通マスターを維持。NGも注意喚起後に配置可能。
- [x] 人員名・メンバーの既存導線に加え、案件軸の候補者・配置済み人員に編集入口。
- [x] 案件軸案件ヘッダー右クリック、人員軸人員ヘッダー右クリックから店舗・代理店側ルール登録。既存の出力メニューは残す。
- [ ] 共通ルール画面の取得404を原因確認して修正。
- [x] 提出シフト数・案件受注数・過不足の3行を両軸の日付見出し直上に固定。集計定義は維持。
- [ ] 本人の希望店舗・NG店舗は店舗側・代理店側ルールと別情報。メンバーから操作し、認証済み本人の直属管理者だけ編集可能。本人・同階層・単に上位・開発者という理由で許可しない。未設定管理者は不許可。
- [ ] 本人の希望と取引先ルールが競合しても両方表示。NGは非推奨で、確認後に配置可能。
- [ ] 回帰テスト、コミット、PR、マージ、公開、本番確認。

## 変更しないもの

- 配布済みURL・ホスト名の変更は9/15以降まで保留。
- 希望休の提出処理・回収データは変更しない。
- 本番の既存アサインや他人のルールをテスト目的で無断変更しない。

## 事前調査

- 本番オーダーで2名時にカレンダー約507→860pxを確認。未保存フォームだけを操作。
- 本番およびコードで税込初期値を確認。
- 人員名から編集画面へ遷移後、JSON以外の404応答を確認（原因未確定）。
- 本番人員軸の先頭3行を確認。本人希望の独立した保存欄は関連コード上で未確認。
